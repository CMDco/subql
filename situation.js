/**
 * 1. Developer must provide us the schema string
 *    1.1 Our registerSchema method will parse out the functions and return types
 * 2. Developer must register resolver methods with const keys
 *    2.1 Via the information we got from 1.1 Cabotage will wrap the functions accordingly
 * 3. Developer can use getRoot/buildSchema to construct their API
 */

/**
 * 1. Client will provide us the query string during subscription
 * 2. Client will also pass a callback that will be called when a subscription notifies
 * 3. Get a bj from a guy because it's not gay
 */

/**
 * 
 */